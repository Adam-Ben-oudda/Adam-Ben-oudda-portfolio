# Core dependencies
numpy>=1.25.0
scipy>=1.11.0

# Logging and utilities
rich>=13.7.0  # Optional: for better console logging
colorlog>=6.8.0  # Optional: colored logs

# For timestamp parsing and safe date operations
python-dateutil>=2.9.0

# Typing and validation (optional for advanced usage or extensions)
pydantic>=2.7.1

# Optional (if using interactive simulation or dashboards in future)
matplotlib>=3.9.0

# Python version (for environments)
python_version>="3.10"
